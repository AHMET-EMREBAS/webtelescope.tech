import { MainPanelDirective } from './main-panel.directive';

describe('MainPanelDirective', () => {
  it('should create an instance', () => {
    const directive = new MainPanelDirective();
    expect(directive).toBeTruthy();
  });
});
