import { StatusBarDirective } from './status-bar.directive';

describe('StatusBarDirective', () => {
  it('should create an instance', () => {
    const directive = new StatusBarDirective();
    expect(directive).toBeTruthy();
  });
});
