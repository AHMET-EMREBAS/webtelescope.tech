import { ToolbarRightDirective } from './toolbar-right.directive';

describe('ToolbarRightDirective', () => {
  it('should create an instance', () => {
    const directive = new ToolbarRightDirective();
    expect(directive).toBeTruthy();
  });
});
