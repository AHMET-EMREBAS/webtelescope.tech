import { StatusBarRightDirective } from './status-bar-right.directive';

describe('StatusBarRightDirective', () => {
  it('should create an instance', () => {
    const directive = new StatusBarRightDirective();
    expect(directive).toBeTruthy();
  });
});
