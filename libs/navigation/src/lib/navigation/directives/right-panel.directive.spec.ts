import { RightPanelDirective } from './right-panel.directive';

describe('RightPanelDirective', () => {
  it('should create an instance', () => {
    const directive = new RightPanelDirective();
    expect(directive).toBeTruthy();
  });
});
