import { LeftPanelDirective } from './left-panel.directive';

describe('LeftPanelDirective', () => {
  it('should create an instance', () => {
    const directive = new LeftPanelDirective();
    expect(directive).toBeTruthy();
  });
});
