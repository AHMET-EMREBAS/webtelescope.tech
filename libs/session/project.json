{
  "name": "session",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/session/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "@nx/js:swc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/libs/session",
        "main": "libs/session/src/index.ts",
        "tsConfig": "libs/session/tsconfig.lib.json",
        "assets": ["libs/session/*.md"]
      }
    },
    "publish": {
      "command": "node tools/scripts/publish.mjs session {args.ver} {args.tag}",
      "dependsOn": ["build"]
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": [
          "libs/session/**/*.ts",
          "libs/session/package.json"
        ]
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "libs/session/jest.config.ts"
      }
    }
  },
  "tags": []
}
