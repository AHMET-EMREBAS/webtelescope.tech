import { PrinterPicker } from '../common-imp';
import { PropertyBuilder } from './property';

export class PropertyPrinterPicker extends PrinterPicker {
  constructor(builder: PropertyBuilder) {
    super(builder);
  }
}
