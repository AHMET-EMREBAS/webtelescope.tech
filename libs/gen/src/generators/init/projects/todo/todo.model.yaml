# yaml-language-server: $schema=./../___schema/model/model.json
properties:
  name:
    type: Role
    isArray: true

relations:
  category:
    target: Category
    type: Many

required:
  - name

unique:
  - name
