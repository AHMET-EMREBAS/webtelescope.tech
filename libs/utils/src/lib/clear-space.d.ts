/**
 * Clear the space in text
 * @param text string
 * @returns string
 */
export declare function clearSpace(text: string): string;
/**
 * Clear all space characters in the text and contact entire string.
 * @param text
 * @returns
 */
export declare function concatAll(text: string): string;
