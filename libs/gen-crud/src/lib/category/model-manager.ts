import { CategoryModel } from '@webpackages/gen-meta';
import { ModelManager } from '@webpackages/meta';

export const categoryModelManager = new ModelManager(CategoryModel);


categoryModelManager.list