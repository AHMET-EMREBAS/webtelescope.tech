{
  "name": "graph",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/graph/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "@nx/js:swc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/libs/graph",
        "main": "libs/graph/src/index.ts",
        "tsConfig": "libs/graph/tsconfig.lib.json",
        "assets": ["libs/graph/*.md"]
      }
    },
    "publish": {
      "command": "node tools/scripts/publish.mjs graph {args.ver} {args.tag}",
      "dependsOn": ["build"]
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["libs/graph/**/*.ts", "libs/graph/package.json"]
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "libs/graph/jest.config.ts"
      }
    }
  },
  "tags": []
}
